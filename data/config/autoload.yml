autoload:

  # no more magic autoloading, wa are using composer now...
  # @see composer.json

  # project files, plugins files and project lib files should be handled by the composer.json
  # of the project itself, no more magic.
